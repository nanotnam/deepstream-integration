[message-broker]
# The NVIDIA Kafka protocol adapter extracts this JSON field and emits it as
# the Kafka record key. Both ALPR event and health contracts require source_id.
partition-key = source_id
