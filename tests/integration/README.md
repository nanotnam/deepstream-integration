# Integration tests

Place portable cross-component tests here. Kafka integration should exercise contract
validation, topic handoff, offset behavior, retry, and dead-letter publication against
the local stack in `kafka/`.
